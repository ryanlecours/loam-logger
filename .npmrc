# npm workspaces configuration
# Only root package-lock.json should exist

# Enable workspaces
workspaces=true

# Hoist everything to root node_modules (no nested node_modules)
install-strategy=hoisted

# Create package-lock.json at root only
package-lock=true

# Don't save exact versions
save-exact=false
