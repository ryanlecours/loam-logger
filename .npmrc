# Ensure package-lock.json is only created at the root
package-lock=true

# Use workspaces
workspaces=true

# Don't create package-lock.json in workspace packages
# (npm should only create/update the root package-lock.json)
save-exact=false
