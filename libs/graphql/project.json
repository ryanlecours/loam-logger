{
  "name": "graphql",
  "projectType": "library",
  "targets": {
    "codegen": {
      "executor": "nx:run-commands",
      "options": {
        "command": "graphql-codegen --config codegen.ts",
        "cwd": "libs/graphql"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
