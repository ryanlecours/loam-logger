query Rides($take: Int, $after: ID, $filter: RidesFilterInput) {
  rides(take: $take, after: $after, filter: $filter) {
    id
    garminActivityId
    stravaActivityId
    whoopWorkoutId
    suuntoWorkoutId
    startTime
    durationSeconds
    distanceMeters
    elevationGainMeters
    averageHr
    rideType
    bikeId
    notes
    trailSystem
    location
  }
}
